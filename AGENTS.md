## Agent skills

### Issue tracker

开发规格和任务使用 GitHub Issues 管理，通过 `gh` CLI 操作。详见
`docs/agents/issue-tracker.md`。

### Triage labels

使用默认五阶段标签：`needs-triage`、`needs-info`、`ready-for-agent`、
`ready-for-human`、`wontfix`。详见 `docs/agents/triage-labels.md`。

### Domain docs

采用单上下文结构，领域词汇记录在根目录 `CONTEXT.md`，架构决策记录在
`docs/adr/`。详见 `docs/agents/domain.md`。
