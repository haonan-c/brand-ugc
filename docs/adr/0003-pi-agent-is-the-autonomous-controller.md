# Pi Agent 作为可扩权的自主总控

Pi Agent 负责自主规划、选择和组合 Skills、拆分步骤、调用子 Agent、失败重规划及按需申请新能力；NestJS、BullMQ 等组件只提供持久化执行、队列、事件、审计、审批和恢复，不用固定状态机预先决定业务流程。Pi 可以自主生成、安装并在隔离环境测试新 Skill，已批准能力可以自主调用，高风险能力需要申请，禁止能力不能授权；新 Skill 进入生产能力池前必须由管理员批准。
